<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('home')}}"><img class="logo-custom" src="{{ asset('uploads/images/logo2.jpg')}}" alt="logo Banlieu's Heart"></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarCollapse">
			<ul class="navbar-nav ml-auto">
				<li class="nav-item ">
					<a class="nav-link" href="{{ path('news_index')}}">Actualités</a>
				</li>
				{% if app.user %}
					{% if app.user.roles[0] is same as "ROLE_ADMIN" %}
						<li class="nav-item ">
							<a class="nav-link" href="{{ path('admin_dashboard') }}">Administration</a>
						</li>
						<li class="nav-item ">
							<a class="nav-link" href="{{ path('news_new') }}">Créer une news</a>
						</li>
					{% endif %}
				{% endif %}

				<li class="nav-item">
					<a class="nav-link" href="#">Galerie</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('contact') }}">Contact</a>
				</li>
				<li class="nav-item active">
					<a class="nav-link" href="{{ path('product_index') }}">Boutique</a>
				</li>
				{% if app.user %}
					{% if app.user.roles[0] is same as "ROLE_ADMIN" %}
						<li class="nav-item ">
							<a class="nav-link" href="{{ path('product_new') }}">Ajouter un produit</a>
						</li>
					{% endif %}
				{% endif %}

				<div class="collapse navbar-collapse" id="navbarNavDarkDropdown">
					<ul class="navbar-nav">
						<li class="nav-item dropdown">
							<div class="dropdown">
								<button class="dropdown-toggle btn-dark" id="navbarDarkDropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
									Connexion/s'enregistrer
								</button>
								<ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="navbarDarkDropdownMenuLink">
									{% if app.user %}
										<li>
											<a class="dropdown-item" href="{{ path('account')}}">Mon compte
												<small>({{ app.user.firstName}})</small>
											</a>
										</li>
										<li>
											<a class="dropdown-item" href="{{ path('app_logout')}}">Se deconnecter</a>
										</li>
									{% else %}
										<li>
											<a class="dropdown-item" href="{{ path('app_login')}}">Se connecter</a>
										</li>
										<li>
											<a class="dropdown-item" href="{{ path('app_register')}}">Creer un compte</a>
										</li>
									{% endif %}
								</ul>
								<a href="{{ path('cart') }}">
									<img src="{{ asset('uploads/images/shopping-cart.png')}}" class="cart-icon" alt="Mon panier">
								</a>
							</div>
						</li>
					</ul>
				</div>
			</ul>
		</div>
	</div>
</nav>
