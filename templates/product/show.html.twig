{% extends 'base.html.twig' %}

{% block title %}
	{{product.name}}
{% endblock %}

{% block content %}
	<div class="row">
		<div class="col-md-5">
			<img src="/uploads/products/{{ product.imageUrl}}" class="img-fluid">
		</div>
		<div class="col-md-7 my-auto">
			<h3>{{ product.name }}</h3>
			<p>{{ product.subtitle }}</p>
			{{ (product.price / 100) |number_format(2, ',', '.' ) }}
			€
			<hr>
			<p>
				{{ product.description }}
			</p>
			<div class="row">
				<a href="{{ path('add_to_cart', {'id' : product.id})}}" class="btn btn-lg btn-dark mt-3">
					Ajouter au panier
				</a>
			</div>
		</div>
	</div>


	{# <a class="btn btn-infos" href="{{ path('product_index') }}">back to list</a>
					
						<a href="{{ path('product_edit', {'id': product.id}) }}">edit</a>
					
						{{ include('product/_delete_form.html.twig') }} #}
{% endblock %}
